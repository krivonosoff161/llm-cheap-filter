# LLM Cheap Filter Agent Contract

- Follow the canonical global agent and Git contracts resolved by the Workbench registry.
- This repository owns deterministic prefiltering and bounded cheap-to-chief triage only. It
  is not a security control, correctness oracle, or portfolio authority.
- Treat `main` as protected; use a registered `codex/*` task worktree and explicit-path staging.
- Repository instructions may narrow but never weaken global secret, money, evidence,
  destructive-action, and owner-gate boundaries.
- Keep secrets, credentials, private evidence, raw model/tool output, runtime data, and
  machine-local paths out of public Git; do not call providers without fresh authority.
- A cheap-model result cannot lower a deterministic guard decision or establish trust.
- Merge, release, deployment, enforcement, providers, and destructive Git remain separate
  owner gates. Portfolio and local roadmap documents grant no authority.
- Before completion run the repository test/quality gates and the portfolio contract test.
